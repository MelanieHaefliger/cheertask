import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove the experimental.appDir property since you're not using it.
  // You can add any other config options here if needed.
};

export default nextConfig;
